{
  "schemaVersion": 1,
  "id": "flight-panel",
  "version": "${version}",
  "name": "Flight Panel",
  "description": "Config screen generation utility",
  "authors": [
    "zenfyr"
  ],
  "contact": {
    "repo": "https://github.com/constellation-mc/flight-panel"
  },
  "license": "MIT",
  "icon": "icon.png",
  "environment": "client",
  "entrypoints": {
    "client": [
      "dev.zenfyr.flightpanel.impl.TestInit"
    ]
  },
  "mixins": [
    "flight-panel.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=${loader_version}",
    "fabric": ">=${fabric_version}",
    "minecraft": "${minecraft_version}",
    "pulsar": ">=${pulsar_version}"
  }
}
